<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= yield(:title) %>Rails Blog Sample</title>
    <%= csrf_meta_tags %>
    <%= favicon_link_tag "/public/apple-touch-icon-precomposed.png", :rel => "apple-touch-icon-precomposed", :type => "image/png" %><!-- 57×57px -->
    <%= favicon_link_tag "/public/favicon.ico", :rel => "shortcut icon" %><!-- 32×32px -->
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>
    <nav class="navbar navbar-inverse navbar-static-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-main-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="<%= root_path %>">Rails Blog Sample</a>
        </div>

        <div id="navbar-main-collapse" class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to "Blog", posts_path %></li>
            <li><%= link_to "About", page_path("about") %></li>
            <li><%= link_to "Feedback", feedback_path %></li>
          </ul>
        </div>
      </div>
    </nav>

    <% if content_for?(:breadcrumbs) %>
      <ul class="breadcrumb container">
        <li><%= link_to "Home", root_path %></li>
        <%= yield(:breadcrumbs) %>
      </ul>
    <% end %>

    <div class="container">
      <% if content_for?(:main_columns) %>
        <%= yield(:main_columns) %>
      <% else %>
        <div class="row">
          <div class="col-md-12">
            <%= bootstrap_flash %>
            <%= yield %>
          </div>
        </div>
      <% end %>

      <footer>
        <ul class="list-inline">
          <li class="pull-right">&copy; Rails Blog Sample, <%= Time.current.year %></li>

          <li><%= link_to "Blog", posts_path %></li>
          <li><%= link_to "About", page_path("about") %></li>
          <li><%= link_to "Feedback", feedback_path %></li>
        </ul>
      </footer>
    </div>
  </body>
</html>
