<% content_for(:title) do %>
  Add new page &middot;
<% end %>

<div class="page-header">
  <h1>Add new page</h1>
</div>

<%= render 'nav' %>
<%= render 'form', page: @page, form_url: admin_pages_path %>
