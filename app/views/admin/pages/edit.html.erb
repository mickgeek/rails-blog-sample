<% content_for(:title) do %>
  Edit page &middot;
<% end %>

<div class="page-header">
  <h1>Edit page</h1>
</div>

<%= render 'nav' %>
<%= render 'form', page: @page, form_url: admin_page_path(@page) %>
